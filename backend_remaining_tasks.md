# 백엔드 미구현 작업 우선순위

> 기준 문서: `강_새로이_최종_기획안_v2.md`와 페이지별 기능 명세
> 현재 상태: BOD 계산, 예산 검증, 익명 응답 저장, 시민 통계, 후보자 기본 집계 구현 완료  
> 목적: 프런트엔드 개발과 실제 배포에 필요한 남은 백엔드 작업을 순서대로 진행

## 우선순위 요약

| 순서 | 작업 | 중요도 | 현재 상태 | 선행 작업 |
|---:|---|---|---|---|
| 1 | PostgreSQL/Supabase 공유 DB 연결 | P0 | 완료 | 없음 |
| 2 | 개인정보 보호 및 운영 오류 처리 보강 | P0 | 완료 | 없음 |
| 3 | 결과 안내와 배지 판정 구현 | P1 | 완료 | 배지 기준 확정 |
| 4 | 후보자 대시보드 필터·페이지네이션 | P1 | 완료 | 없음 (1번 완료) |
| 5 | 돌발상황 기능 구현 | P2 | 완료 | - |
| 6 | 시민 의견 키워드 분석 | P3 | 미구현·선택 | 1번, 4번 |
| 7 | 전체 통합 테스트 및 배포 점검 | P0 | 진행 중 | 1~4번 |

---

# 1. PostgreSQL/Supabase 공유 DB 연결

## 현재 상태

- [x] Supabase REST 저장소 구현
- [x] SQLite/Supabase 실행 환경 선택
- [x] `.env` 로딩과 `.gitignore` 보호
- [x] Secret key와 레거시 `service_role` 호환
- [x] 1,000건 초과 페이지 조회
- [x] DB 장애 시 안전한 `503` 응답
- [x] Supabase 단위·통합 테스트
- [x] 사용자가 Supabase 프로젝트 생성
- [x] SQL Editor에서 마이그레이션 실행
- [x] `backend/.env`에 프로젝트 URL과 Secret key 입력
- [x] 실제 Supabase를 사용한 저장·재조회 검증

## 실제 연결 검증 결과

2026-08-13 기준 실제 Supabase 프로젝트에서 다음 항목을 확인했습니다.

- `responses` 테이블 읽기 성공
- 합성 테스트 응답 저장 성공
- 별도 저장소 인스턴스에서 같은 응답 재조회 성공
- `POST /api/responses` 응답 `201`
- 저장 직후 `GET /api/stats` 반영 및 응답 `200`
- 저장 직후 `GET /api/candidate/report` 반영 및 응답 `200`
- 테스트 응답 삭제 후 데이터 0건 복구
- Secret key와 프로젝트 URL이 Git에 포함되지 않음

## 작업 목적

현재 코드는 로컬 SQLite와 Supabase 공유 DB를 선택할 수 있으며, 계정 설정·SQL 마이그레이션·실제 저장 및 재조회 검증까지 완료했습니다.

## 구현할 내용

- `ResponseRepository` 규약을 구현하는 Supabase REST 저장소 추가
- `SUPABASE_URL`, `SUPABASE_SECRET_KEY` 환경변수 사용
- 서버 시작 시 SQLite 또는 Supabase 저장소 선택
- Supabase SQL Editor용 PostgreSQL 마이그레이션 제공
- 연결 실패, 저장 실패, 조회 실패 처리
- Secret key와 접속 정보를 코드나 Git에 저장하지 않기
- 전체 통계를 위한 페이지 단위 조회

## 수정·추가 예상 파일

- `backend/river_api/supabase_repository.py`
- `backend/river_api/config.py`
- `backend/river_api/__main__.py`
- `backend/river_api/application.py`
- `backend/.env.example`
- `backend/migrations/001_postgresql_responses.sql`
- `backend/README.md`
- `backend/SUPABASE_SETUP.md`

## 완료 조건

- 두 개 이상의 서버 프로세스에서 같은 참여 데이터 조회 가능
- 서버를 재시작해도 데이터 유지
- 응답 저장 직후 `/api/stats`와 `/api/candidate/report`에 반영
- DB 접속 실패 시 JSON 형식의 `503` 응답 반환
- PostgreSQL 연동 통합 테스트 통과

---

# 2. 개인정보 보호 및 운영 오류 처리 보강

## 현재 상태

- [x] 이메일·휴대전화·유선전화 패턴 차단
- [x] 주민등록번호 형태 차단
- [x] 명시적으로 입력한 실명과 상세 주소 형태 차단
- [x] Unicode 정규화, 공백·제어문자 정리
- [x] HTML 태그 입력 거부
- [x] 게임 설정 API에 개인정보 입력 금지 안내 제공
- [x] 모든 오류를 공통 JSON 구조로 통일
- [x] 잘못된 HTTP 메서드에 `405`와 `Allow` 헤더 반환
- [x] JSON이 아닌 POST 요청에 `415` 반환
- [x] 16KiB 초과 요청에 `413` 반환
- [x] DB 장애에 안전한 `503` 반환
- [x] 예상하지 못한 오류에 내부 정보를 숨긴 `500` 반환
- [x] 모든 HTTP 응답에 `X-Request-ID` 제공
- [x] 개인정보·키·DB 접속 정보를 제외한 안전 로그
- [x] 단위·통합·실제 HTTP 테스트

## 실제 HTTP 검증 결과

2026-08-13 기준 실제 Supabase 설정으로 다음 항목을 확인했습니다.

- 정상 상태 조회 `200`
- 잘못된 메서드 `405 METHOD_NOT_ALLOWED`
- 잘못된 Content-Type `415 UNSUPPORTED_MEDIA_TYPE`
- 잘못된 JSON `400 INVALID_JSON`
- 개인정보 포함 의견 `400 PERSONAL_INFORMATION_NOT_ALLOWED`
- 요청 크기 초과 `413 PAYLOAD_TOO_LARGE`
- 모든 오류의 응답 본문 `requestId`와 `X-Request-ID` 헤더 일치
- 거부된 의견이 Supabase에 저장되지 않음
- 자동 테스트 26개 통과

## 작업 목적

현재 이메일과 휴대전화 패턴은 차단하지만 이름, 상세 주소, 주민등록번호 등의 개인정보가 자유 의견에 들어오는 상황을 완전히 방지하지는 못합니다. 또한 예상하지 못한 서버 오류를 일관된 JSON 응답으로 처리해야 합니다.

## 구현할 내용

### 개인정보 검사

- 주민등록번호 형태 차단
- 상세 주소로 의심되는 표현 검사 또는 안내 강화
- 의견 앞뒤 공백과 제어문자 제거
- HTML 태그 또는 위험한 제어문자 정리
- 개인정보가 감지되면 저장하지 않고 명확한 오류 코드 반환
- 개인정보 필터의 한계는 화면 안내 문구로 보완

### 운영 오류 처리

- DB 오류와 예상하지 못한 오류를 공통 오류 응답으로 변환
- 내부 예외 메시지와 DB 정보가 사용자에게 노출되지 않게 처리
- 잘못된 HTTP 메서드에 `405` 반환
- 지원하지 않는 Content-Type 처리
- 요청 ID를 생성해 오류 로그와 응답을 연결

## 권장 오류 코드

| HTTP | 코드 | 상황 |
|---:|---|---|
| 400 | `VALIDATION_ERROR` | 입력값 오류 |
| 400 | `PERSONAL_INFORMATION_NOT_ALLOWED` | 개인정보 포함 의견 |
| 404 | `NOT_FOUND` | 존재하지 않는 API |
| 405 | `METHOD_NOT_ALLOWED` | 지원하지 않는 메서드 |
| 413 | `PAYLOAD_TOO_LARGE` | 요청 크기 초과 |
| 503 | `DATABASE_UNAVAILABLE` | DB 연결·조회 실패 |
| 500 | `INTERNAL_SERVER_ERROR` | 예상하지 못한 오류 |

## 수정·추가 예상 파일

- `backend/river_api/service.py`
- `backend/river_api/application.py`
- `backend/river_api/http_server.py`
- `backend/tests/test_api.py`

## 완료 조건

- [x] 이메일, 휴대전화, 주민등록번호 형태가 포함된 의견 저장 차단
- [x] 허용하지 않는 요청 필드 저장 차단
- [x] DB 오류가 발생해도 서버 프로세스가 종료되지 않음
- [x] 모든 오류가 동일한 JSON 구조로 반환됨
- [x] 관련 자동 테스트 통과

---

# 3. 결과 안내와 배지 판정 구현

## 현재 상태

- [x] `TRY_AGAIN`, `MISSION_COMPLETE`, `PERFECT_CLEAR` 결과 상태 판정
- [x] 결과 상태별 제목과 안내 문구 제공
- [x] 확정된 6종 배지 ID·이름·설명 제공
- [x] 선택한 각 정책의 장점 설명 제공
- [x] 실패 시 목표까지 남은 BOD 제공
- [x] 남은 예산으로 추가 가능한 직접 수질개선 정책 최대 3개 추천
- [x] 추천 적용 후 예상 BOD·등급·결과 상태·잔여 예산 제공
- [x] 전체 정책 조합으로 추천 예산·중복·예상값 검증

## 검증 결과

2026-08-13 기준 다음 항목을 확인했습니다.

- 자동 테스트 35개 통과
- 3개 하천의 예산 내 정책 조합 351개 전수 검사
- 추천 정책에 현재 선택 정책이 중복되지 않음
- 추천 정책 비용이 잔여 예산을 초과하지 않음
- 추천 적용 후 예상 BOD·등급·상태·잔여 예산이 실제 재계산과 일치
- 실패와 퍼펙트 결과의 실제 HTTP JSON 응답 확인
- BOD가 줄지 않은 실패와 `약간좋음(II)`까지 개선된 실패의 안내 문구 구분

## 작업 목적

현재 서버는 성공 여부와 목표까지 남은 BOD를 제공하지만 결과 화면에 필요한 `잘한 선택`, `부족한 정책`, `추천 정책`, `획득 배지`를 제공하지 않습니다.

## 확정된 배지 기준

2026-08-13 기획 담당자가 아래 기준과 퍼펙트 달성 시 `좋음 달성`, `1mg 퍼펙트` 배지를 함께 지급하는 방식을 승인했습니다.

| 배지 | 권장 조건 |
|---|---|
| 좋음 달성 | 최종 BOD 2.0mg/L 이하 |
| 1mg 퍼펙트 | 최종 BOD 1.0mg/L 이하 |
| 생태 회복 | 생태 점수 70점 이상 |
| 시민 공감 | 시민 만족도 70점 이상 |
| 스마트 관리 | 관리 능력 70점 이상 |
| 알뜰 정책 | 미션 성공이며 남은 예산 20억원 이상 |

## 구현할 내용

- [x] 결과 상태를 `TRY_AGAIN`, `MISSION_COMPLETE`, `PERFECT_CLEAR`로 제공
- [x] 획득 배지 ID와 이름 제공
- [x] 선택 정책의 장점 설명 제공
- [x] 실패 시 목표까지 남은 BOD 제공
- [x] 현재 예산으로 선택 가능한 추천 정책 계산
- [x] 추천 정책을 선택했을 때 예상 BOD와 등급 제공
- [x] 추천이 실제 예산과 중복 선택 규칙을 위반하지 않도록 처리

## API 응답 추가 예시

```json
{
  "resultStatus": "TRY_AGAIN",
  "badges": [],
  "strengths": ["관리 능력이 향상되었습니다."],
  "recommendations": [
    {
      "policyId": "sourceBlock",
      "reason": "오염원 유입을 직접 줄여 목표 등급에 가까워집니다.",
      "expectedFinalBod": 1.8,
      "expectedFinalGrade": "Ib"
    }
  ]
}
```

## 수정·추가 예상 파일

- `backend/river_api/domain.py`
- `backend/openapi.yaml`
- `backend/tests/test_domain.py`

## 완료 조건

- [x] 실패·성공·퍼펙트 상태가 정확하게 구분됨
- [x] 모든 배지가 확정된 기준대로 판정됨
- [x] 추천 정책이 예산을 초과하지 않음
- [x] 추천 정책 적용 후의 예상값이 실제 시뮬레이션 결과와 일치
- [x] 프런트가 별도 계산 없이 결과 화면을 구성 가능

---

# 4. 후보자 대시보드 필터와 페이지네이션

## 현재 상태

- [x] 후보자 리포트의 하천·지역·UTC 날짜 필터
- [x] 시작일·종료일을 모두 포함하는 날짜 범위 처리
- [x] 필터 조합과 빈 결과 처리
- [x] 시민 의견 전용 API
- [x] 의견 페이지네이션과 최신순·오래된 순 정렬
- [x] 전체 의견 수·전체 페이지 수·이전/다음 페이지 여부 제공
- [x] 하천별·지역별 최우선 정책 집계
- [x] SQLite·Supabase 필터 및 페이지 조회 구현
- [x] 기존 Supabase 프로젝트용 조회 인덱스 마이그레이션 제공

## 검증 결과

2026-08-13 기준 다음 항목을 확인했습니다.

- 자동 테스트 43개 통과
- 55개 의견을 20개씩 조회해 3페이지와 마지막 15개 확인
- 최신순·오래된 순 정렬과 하천 필터 확인
- 의견이 없는 응답은 의견 전체 개수에서 제외
- UTC 날짜의 시작 시각과 종료일 마지막 시각 포함 확인
- 잘못된 하천·지역·날짜·페이지·정렬·중복 조건 거부
- OpenAPI 0.3.0 YAML 파싱과 로컬 참조 56개 확인
- 실제 Supabase에서 미래 날짜 빈 범위의 조합 필터와 의견 페이지를 읽기 전용으로 검증
- Supabase 의견 개수 1,025건 조회 시 1,000건 제한 이후 다음 페이지 집계 확인

## 작업 목적

현재 후보자 API는 전체 집계와 최신 의견 50개를 한 번에 제공합니다. 참여 데이터가 많아지면 하천·지역·기간별 분석과 의견 목록 탐색이 어렵습니다.

## 구현할 내용

- [x] 후보자 통계 API에 조회 조건 추가
  - 하천
  - 지역
  - 시작 날짜
  - 종료 날짜
- [x] 시민 의견 전용 조회 API 추가
- [x] 의견 목록 페이지네이션 추가
- [x] 정렬 기준 제공
  - 최신순
  - 오래된 순
- [x] 전체 의견 수와 다음 페이지 존재 여부 제공
- [x] 하천별·지역별 우선 정책 집계
- [x] 잘못된 날짜나 페이지 값 검증

## 권장 API

```text
GET /api/candidate/report?riverId=dongcheon&district=busanjin&from=2026-08-01&to=2026-08-31
GET /api/candidate/comments?riverId=dongcheon&page=1&pageSize=20&sort=latest
```

## 수정·추가 예상 파일

- `backend/river_api/http_server.py`
- `backend/river_api/application.py`
- `backend/river_api/service.py`
- `backend/river_api/repository.py`
- `backend/river_api/statistics.py`
- `backend/openapi.yaml`
- `backend/tests/test_api.py`

## 완료 조건

- [x] 하천·지역·기간 필터가 각각 및 조합으로 작동
- [x] 빈 결과도 오류가 아닌 정상 빈 통계로 반환
- [x] 의견이 50개를 넘어도 페이지를 이동해 조회 가능
- [x] 필터가 적용된 집계의 분모와 비율이 정확함
- [x] 필터·페이지네이션 자동 테스트 통과

---

# 5. 돌발상황 기능

## 중요도

기획안에서 `시간이 남으면` 구현하는 선택 기능입니다. 1~4번을 완료하기 전에는 착수하지 않습니다.

## 구현 결과

- 정책 2개 이상에서 이벤트를 활성화하고, 미선택 계산은 `PENDING`으로 반환
- 최종 제출에서는 활성화된 이벤트 선택을 필수로 검증
- 센서 선택 시 조사 비용 0억원·관리 +15·시민 +5, 미선택 시 비용 5억원·관리 +10·시민 +5 적용
- 지켜보기 선택 시 시민 -10과 `WORRIED` 임시 상태 적용
- SQLite·Supabase에 이벤트 ID·선택·비용·점수 효과 저장
- 시민 통계와 후보자 리포트에 이벤트 선택률·센서 활용 조사율 포함

> 로컬 SQLite 저장 검증과 전체 자동 테스트를 완료했습니다. 현재 연결된
> Supabase에서도 이벤트 컬럼 조회·응답 매핑·이벤트 통계 집계가 정상 동작합니다.

## 구현할 내용

- 정책을 두 개 선택한 시점에 이벤트 대상 여부 계산
- 한 번의 도전에서 같은 이벤트가 중복 적용되지 않게 처리
- 선택지 구현
  - 추가 수질조사
  - 일단 지켜보기
- 스마트 수질센서 선택 여부에 따른 비용·효과 분기
- 이벤트 비용을 포함한 최종 예산 검증
- 관리 능력과 시민 만족도 반영
- 선택한 이벤트와 결과 저장
- 통계에 이벤트 선택률을 포함할지 결정

## 권장 요청 구조

```json
{
  "riverId": "dongcheon",
  "policyIds": ["sewer", "sensor"],
  "eventChoice": "INVESTIGATE"
}
```

## 저장할 추가 데이터

- `eventId`
- `eventChoice`
- `eventCost`
- `eventScoreEffects`

## 수정·추가 예상 파일

- `backend/river_api/domain.py`
- `backend/river_api/service.py`
- `backend/river_api/repository.py`
- `backend/river_api/statistics.py`
- `backend/migrations/003_add_event_result.sql`
- `backend/openapi.yaml`
- `backend/tests/test_domain.py`
- `backend/tests/test_api.py`

## 완료 조건

- [x] 센서 선택 시 조사 비용이 0억원으로 처리됨
- [x] 센서 미선택 시 조사 비용 5억원 반영
- [x] 지켜보기 선택 시 시민 만족도 10점 감소
- [x] 이벤트 비용을 포함해 총예산 100억원을 넘길 수 없음
- [x] 저장 후 다시 조회해도 이벤트 결과가 유지됨

---

# 6. 시민 의견 키워드 분석

## 중요도

MVP 완성 후 추가하는 선택 기능입니다. 처음에는 AI 없이 규칙 기반으로 구현하는 것을 권장합니다.

## 구현할 내용

- 의견을 다음 범주로 분류
  - 악취
  - 오염원·생활하수
  - 데이터 공개
  - 생태 복원
  - 산책로·편의시설
  - 기타
- 한 의견에 여러 범주 허용 여부 결정
- 키워드별 의견 수와 비율 집계
- 후보자 리포트에 주요 키워드 제공
- 분류되지 않은 원문을 삭제하거나 변경하지 않기

## 구현 방법 권장 순서

1. 한국어 키워드 사전을 이용한 규칙 기반 분류
2. 실제 의견을 확인해 키워드 보완
3. 필요할 때만 AI 요약 기능 검토

## 완료 조건

- 같은 입력은 항상 같은 범주로 분류됨
- 분류 실패 시 `기타`로 안전하게 처리
- 키워드 통계가 전체 의견 수와 일치
- AI 기능이 실패해도 기존 통계와 원문 조회에 영향이 없음

---

# 7. 전체 통합 테스트 및 배포 점검

## 작업 목적

새 기능이 각각 작동하는 것뿐 아니라 `게임 설정 → 계산 → 저장 → 통계 → 후보자 리포트` 전체 흐름이 실제 배포 환경에서도 이어지는지 확인합니다.

## 필수 테스트 시나리오

1. 동천에서 75억원 조합으로 `좋음(Ib)` 성공
2. 각 하천에서 퍼펙트 조합 성공
3. 센서·모니터링·워킹로드만 선택하면 BOD가 감소하지 않음
4. 100억원을 초과하는 조합 거부
5. 클라이언트가 `finalBod`를 조작해 보내면 거부
6. 익명 집계에 동의하지 않으면 저장하지 않음
7. 개인정보가 포함된 의견 저장 거부
8. 저장 결과가 시민 통계에 즉시 반영
9. 저장 결과가 후보자 리포트에 즉시 반영
10. 날짜·하천·지역 필터의 집계 결과 확인
11. DB 연결 실패 시 안전한 오류 응답 확인
12. 서버 재시작 후 참여 데이터 유지 확인

## 배포 전 확인사항

- `DATABASE_URL` 설정
- `ALLOWED_ORIGINS`를 실제 프런트 주소로 제한
- 데모 데이터 안내 문구 노출
- 실제 과학적 예측이 아니라는 안내 노출
- 개인정보 비수집 안내 노출
- 통계가 부산 시민 전체를 대표하지 않는다는 안내 노출
- 실제 DB 백업 정책 확인
- 로그에 시민 의견 원문과 DB 비밀번호를 남기지 않기

## 최종 완료 조건

- 자동 테스트 전체 통과
- 실제 HTTP 통합 테스트 통과
- 프런트엔드에서 OpenAPI 계약대로 연동 가능
- 배포 환경에서 저장·통계·리포트 전체 흐름 확인
- 필수 안내 문구가 모든 관련 화면에 표시됨

---

# 이번 MVP의 작업 경계

## 백엔드에서 구현할 것

- 서버 권한 BOD·예산·점수 계산
- 익명 응답 저장
- 시민 통계와 후보자 집계
- 공유 DB 연결
- 개인정보 입력 차단
- 결과 안내·배지·추천 데이터
- 후보자 필터와 의견 페이지네이션

## 프런트엔드에서 구현할 것

- 캐릭터와 등급별 표정
- BOD 게이지 이동 애니메이션
- 정책 카드 선택·취소 표현
- 성공·실패 화면 전환
- 통계 차트 표현
- 로딩·빈 데이터·오류 화면
- 브라우저 인쇄와 PDF 저장 레이아웃

## 이번 MVP에서 제외할 것

- 로그인과 회원가입
- 관리자 권한 시스템
- 실제 수질센서 연동
- 실제 BOD 예측 모델
- 정교한 중복 참여 방지
- 이메일 자동 발송

---

# 개발자가 바로 시작할 작업

다음 작업은 추가 기획 결정 없이 바로 시작할 수 있습니다.

1. PostgreSQL 저장소 구현
2. 공통 서버 오류 응답 구현
3. 개인정보 검사 테스트 보강
4. 후보자 의견 페이지네이션 설계

다음 작업은 기획 기준을 확정한 뒤 시작합니다.

1. 배지별 획득 조건
2. 실패 결과의 추천 정책 문구
3. 돌발상황을 MVP에 포함할지 여부
4. 시민 의견 키워드의 최종 분류 기준

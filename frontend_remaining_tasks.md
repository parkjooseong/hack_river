# 프론트엔드 구현 작업 우선순위

> 기준 문서: `강_새로이_최종_기획안_v2.md`, `backend/openapi.yaml`  
> 현재 상태: 프론트엔드 1번 기반 구성 완료, 백엔드 API 구현 완료  
> 목적: 디자인이 확정되기 전에도 기능 기반을 완성하고, 디자인 전달 후 화면만 빠르게 적용할 수 있게 준비

## 기본 구현 원칙

- React + TypeScript + Vite 기반 모바일 웹으로 구현합니다.
- 디자인·검수 기준 화면은 `412 × 917px`입니다.
- 412px 이하에서는 화면 너비에 맞춰 줄이고, 412px 초과 화면에서는 앱 영역을 최대 412px로 제한해 가운데 배치합니다.
- 긴 화면은 세로 스크롤을 허용하고 가로 스크롤은 만들지 않습니다.
- BOD, 등급, 예산, 점수, 결과 판정은 프론트에서 임의 계산하지 않고 백엔드 응답을 기준으로 표시합니다.
- 색상·글꼴·간격·모서리·그림자는 CSS 변수로 분리해 디자인 확정 후 한곳에서 교체합니다.
- 디자인 전에는 임시 색상과 단순 도형만 사용하며, 최종 UI로 확정하지 않습니다.
- 프론트에는 Supabase Secret key 등 비밀값을 넣지 않습니다.

## 우선순위 요약

| 순서 | 작업 | 중요도 | 디자인 필요 여부 | 현재 상태 | 선행 작업 |
|---:|---|---|---|---|---|
| 1 | 프로젝트 기반과 개발 환경 구성 | P0 | 불필요 | 완료 | 없음 |
| 2 | API 계약·타입·통신 계층 구성 | P0 | 불필요 | 미착수 | 1번 |
| 3 | 공통 모바일 레이아웃과 UI 뼈대 구성 | P0 | 불필요 | 미착수 | 1번 |
| 4 | 시민용 핵심 화면과 이동 흐름 구현 | P0 | 불필요 | 미착수 | 2·3번 |
| 5 | 챌린지 상태 관리와 제출 흐름 구현 | P0 | 불필요 | 미착수 | 2·4번 |
| 6 | 시민 통계와 후보자 대시보드 구현 | P1 | 불필요 | 미착수 | 2·3번 |
| 7 | 캐릭터·애니메이션 교체 구조 준비 | P1 | 일부 필요 | 미착수 | 3·5번 |
| 8 | 자동 테스트·접근성·반응형 검증 | P0 | 불필요 | 미착수 | 4~7번 |
| 9 | 최종 디자인 적용 | P1 | 필요 | 미착수 | 디자이너 산출물, 3~7번 |
| 10 | 배포 연동과 운영 점검 | P0 | 최종 디자인 권장 | 미착수 | 8·9번 |

---

# 1. 프로젝트 기반과 개발 환경 구성

## 현재 상태

- [x] React + TypeScript + Vite 프로젝트 구성
- [x] Node.js 24.13.0과 npm 11.6.2 기준 고정
- [x] 개발·빌드·테스트·lint·타입·포맷 검사 명령 구성
- [x] TypeScript strict 모드와 ESLint·Prettier 구성
- [x] React Router 기반 5개 라우트 구성
- [x] 404 화면, 라우트 오류 화면, 최상위 오류 경계 구성
- [x] 페이지 단위 lazy loading 구성
- [x] `VITE_API_BASE_URL` 환경변수 예시 작성
- [x] `.env`, 빌드 결과, 의존성 폴더 Git 제외
- [x] 설치·실행·검사 방법 README 작성

## 검증 결과

2026-08-13 기준 다음 항목을 확인했습니다.

- npm 패키지 보안 검사 보고 0건
- `npm run check` 통과
  - ESLint 통과
  - TypeScript 검사 통과
  - 테스트 파일이 없는 현재 단계에서 Vitest 정상 종료
- `npm run build` 통과
- 5개 페이지와 404 페이지가 각각 별도 lazy chunk로 생성됨
- 개발 서버에서 `/`, `/select`, `/challenge/dongcheon`, `/stats`, `/candidate`, 임의 주소 직접 접근 확인
- 로컬 `.env`와 `node_modules`, `dist`가 Git 추적 대상에서 제외됨

## 작업 목적

디자인 파일을 기다리는 동안 실행·빌드·라우팅이 가능한 프론트엔드 프로젝트를 먼저 만듭니다.

## 구현할 내용

- `frontend/` 폴더에 React + TypeScript + Vite 프로젝트 구성
- Node.js 버전과 패키지 매니저 기준 고정
- 개발·빌드·테스트·검사 명령 구성
- TypeScript strict 모드 적용
- ESLint와 기본 코드 포맷 규칙 구성
- React Router 기반 라우트 구성
  - `/`
  - `/select`
  - `/challenge/:river`
  - `/stats`
  - `/candidate`
- 존재하지 않는 주소용 404 화면 구성
- 페이지 단위 lazy loading과 공통 오류 경계 구성
- 환경변수 예시 파일 작성
  - `VITE_API_BASE_URL`
- `.env`가 Git에 포함되지 않도록 `.gitignore` 적용
- 프론트엔드 README에 실행 방법 작성

## 권장 폴더 구조

```text
frontend/
├─ src/
│  ├─ app/          # 라우터, 전역 설정
│  ├─ api/          # API 클라이언트와 타입
│  ├─ components/   # 공통 UI
│  ├─ features/     # challenge, stats, candidate
│  ├─ pages/        # 라우트 화면
│  ├─ styles/       # 토큰, reset, 전역 스타일
│  ├─ test/         # 테스트 설정과 mock
│  └─ assets/       # 이미지, SVG, 아이콘
├─ .env.example
├─ package.json
└─ README.md
```

## 완료 조건

- [x] 새 환경에서 설치 후 개발 서버가 실행됨
- [x] 5개 라우트와 404 화면에 접근 가능
- [x] 빌드·타입 검사·린트 명령이 모두 통과함
- [x] API 주소를 코드 수정 없이 환경변수로 변경 가능
- [x] Git에 비밀값이나 로컬 `.env`가 포함되지 않음

---

# 2. API 계약·타입·통신 계층 구성

## 작업 목적

화면과 서버 통신을 분리해 디자인이 바뀌어도 API 연동 코드를 다시 작성하지 않게 합니다.

## 연동할 백엔드 API

| 기능 | 메서드와 경로 | 사용하는 화면 |
|---|---|---|
| 서버 상태 | `GET /health` | 배포 점검 |
| 게임 설정 | `GET /api/game/config` | 하천 선택, 챌린지, 설문 |
| 정책 조합 계산 | `POST /api/simulations` | 챌린지 |
| 익명 결과 저장 | `POST /api/responses` | 결과·의견 제출 |
| 시민 통계 | `GET /api/stats` | 시민 통계 |
| 후보자 리포트 | `GET /api/candidate/report` | 후보자 대시보드 |
| 후보자 의견 | `GET /api/candidate/comments` | 후보자 의견 목록 |

## 구현할 내용

- `backend/openapi.yaml`을 기준으로 요청·응답 TypeScript 타입 구성
- 공통 `fetch` 클라이언트 구현
- JSON 요청, API base URL, 요청 취소, 타임아웃 처리
- 공통 오류 모델 구현
  - HTTP 상태
  - 백엔드 오류 코드
  - 사용자용 메시지
  - `requestId`
- 네트워크 오류와 백엔드 검증 오류를 구분
- 중복 요청과 이전 응답 덮어쓰기를 막는 요청 취소 처리
- 조회 데이터의 로딩·성공·빈 결과·오류 상태 표준화
- 실제 서버 없이 화면 개발이 가능하도록 고정 mock 응답 작성
- OpenAPI가 변경되면 타입 차이를 발견할 수 있는 검사 추가

## 구현 원칙

- 프론트는 BOD·등급·예산·배지·추천 정책을 다시 계산하지 않습니다.
- 사용자에게 내부 서버 오류 메시지나 접속 정보를 그대로 보여주지 않습니다.
- 오류 문의에 사용할 수 있도록 `requestId`는 오류 상세 영역에서 확인 가능하게 합니다.
- `POST /api/responses` 중복 제출을 막기 위해 제출 중 버튼을 비활성화합니다.

## 완료 조건

- 모든 API의 정상·오류 mock 응답을 호출할 수 있음
- 화면 컴포넌트가 URL이나 `fetch` 세부 구현을 직접 알지 않음
- 잘못된 입력, DB 장애, 서버 장애, 네트워크 단절을 구분해 처리함
- OpenAPI 계약과 프론트 타입이 어긋나면 검사에서 발견됨

---

# 3. 공통 모바일 레이아웃과 UI 뼈대 구성

## 작업 목적

최종 디자인을 고정하지 않고도 모든 화면이 같은 규격과 공통 상태를 사용하게 합니다.

## 구현할 내용

- 모바일 앱 컨테이너 구현
  - `width: 100%`
  - `max-width: 412px`
  - `min-height: 100dvh`
  - 데스크톱에서 가운데 정렬
  - 가로 스크롤 방지
- 모바일 안전 영역 적용
  - `env(safe-area-inset-top)`
  - `env(safe-area-inset-bottom)`
- 최소 좌우 여백 16px와 최소 터치 영역 44×44px 적용
- CSS 디자인 토큰 구성
  - 색상
  - 글꼴 크기와 굵기
  - 간격
  - 모서리
  - 그림자
  - 애니메이션 시간
- 디자인을 받으면 교체할 공통 컴포넌트 뼈대 구현
  - Button
  - Card
  - Badge
  - Progress/Gauge
  - Modal
  - Input/Textarea/Select
  - Loading
  - EmptyState
  - ErrorState
  - Notice
- 화면별 공통 헤더, 뒤로 가기, 본문, 하단 주요 버튼 영역 구성
- 실제 예측이 아니라는 안내, 데모 데이터, 익명 수집 안내를 표시할 공통 영역 구성

## 디자인 전달 시 교체 지점

- 토큰값: 색상, 타이포그래피, 간격, 그림자
- 공통 컴포넌트 외형
- 캐릭터와 배경 이미지
- 아이콘
- 화면별 세부 배치

라우팅, API, 상태 관리와 도메인 타입은 디자인 적용 시 변경하지 않는 것을 목표로 합니다.

## 완료 조건

- 320px부터 412px까지 가로 스크롤이 발생하지 않음
- 412px보다 넓은 화면에서 앱 영역이 가운데 정렬됨
- 긴 콘텐츠가 잘리지 않고 세로 스크롤됨
- 공통 UI가 키보드와 터치로 모두 조작 가능함
- 최종 디자인 토큰을 한곳에서 교체 가능함

---

# 4. 시민용 핵심 화면과 이동 흐름 구현

## 작업 목적

디자인 없이도 텍스트·버튼·임시 카드로 전체 시민 여정을 끝까지 이동할 수 있게 합니다.

## 구현할 화면

### `/` 랜딩

- 서비스명 `강 새로이`
- 서비스 소개와 성공 기준 안내
- 체험용 시뮬레이션 안내
- `하천 구하러 가기` 버튼

### `/select` 하천 선택

- 동천·괴정천·온천천 카드
- 시작 BOD, 수질등급, 난이도
- 임시 캐릭터 영역
- 선택한 하천의 챌린지로 이동

### `/challenge/:river` 챌린지

- 유효하지 않은 하천 ID 처리
- 현재·목표 BOD와 수질등급
- BOD 게이지
- 생태·시민 만족도·관리 능력
- 남은 예산
- 정책 7개 선택·취소
- 돌발상황 선택
- 결과 화면
- 배지·추천 정책·공약 연계도
- 익명 의견 제출 폼
- 다시 도전, 통계 이동

### `/stats` 시민 통계

- 전체 참여자와 하천별 참여
- 정책 선택률
- 좋음 이상·퍼펙트 달성률
- 평균 등급 개선
- 시민 최우선 문제와 익명 의견
- 데이터 비대표성·데모 데이터 안내

### 공통 화면 상태

- 최초 로딩
- 새로고침
- 데이터 없음
- 네트워크 오류
- 서버 오류
- 잘못된 주소

## 완료 조건

- `/`에서 시작해 `/select`와 챌린지를 거쳐 `/stats`까지 이동 가능
- URL 직접 접근과 새로고침에서도 화면이 복구됨
- 로딩·빈 결과·오류 상황에서도 빈 화면이 나타나지 않음
- 브라우저 뒤로 가기가 예상대로 작동함

---

# 5. 챌린지 상태 관리와 제출 흐름 구현

## 작업 목적

가장 복잡한 게임 진행 상태를 화면 디자인과 분리하고, 빠른 선택에도 서버 결과가 뒤섞이지 않게 합니다.

## 권장 진행 상태

```text
설정 로딩
  → 정책 선택 중
  → 돌발상황 대기/선택
  → 결과 확인
  → 익명 의견 작성
  → 제출 중
  → 제출 완료
```

## 구현할 내용

- 선택한 하천과 정책 선택 순서 관리
- 정책 카드 선택·취소 시 `/api/simulations` 호출
- 서버가 반환한 BOD, 등급, 점수, 예산으로 화면 갱신
- 빠른 연속 선택 시 오래된 API 응답 무시
- 예산 초과와 필수 이벤트 미선택 오류 안내
- 정책 2개 선택 후 돌발상황을 한 번만 노출
- 실패·성공·퍼펙트 결과 상태 분기
- 배지, 강점, 추천 정책 표시
- 다시 도전 시 상태 완전 초기화
- 설문값 관리
  - 최우선 정책
  - 지역
  - 한 줄 의견
  - 익명 집계 동의
- 개인정보 포함 의견 오류를 입력란 가까이에 표시
- 제출 성공 후 중복 제출 방지
- 새로고침·뒤로 가기 시 진행 상태 처리 방침 적용
  - 기본 권장안: 진행 중 상태는 `sessionStorage`에만 임시 저장
  - 제출 완료 데이터와 시민 의견은 브라우저에 영구 저장하지 않음

## 완료 조건

- 정책 선택과 취소 결과가 항상 최신 서버 계산과 일치함
- 예산 초과 조합을 확정할 수 없음
- 돌발상황 비용과 효과가 서버 응답대로 반영됨
- 제출 버튼을 여러 번 눌러도 중복 요청이 발생하지 않음
- 개인정보 거부·DB 장애·네트워크 오류 후 사용자가 안전하게 재시도 가능
- 제출 성공 직후 시민 통계에서 결과를 확인할 수 있음

---

# 6. 시민 통계와 후보자 대시보드 구현

## 작업 목적

백엔드 집계 데이터를 별도 계산 없이 시각화하고 후보자용 인쇄 리포트까지 준비합니다.

## `/stats` 구현 내용

- 핵심 수치 카드
- 하천별 참여율
- 정책별 선택률 순위
- 좋음 이상 달성률과 퍼펙트 달성률
- 평균 등급 개선
- 시민 의견 키워드 통계
- 최신 익명 의견
- 데이터가 0건일 때의 안내
- 실제 부산 시민 전체를 대표하지 않는다는 안내

## `/candidate` 구현 내용

- 총 참여자, 오늘 참여자, 성공률, 퍼펙트율 요약
- 하천별·지역별 정책 통계
- 하천·지역·UTC 날짜 범위 필터
- 필터 초기화와 빈 결과 처리
- 시민 의견 최신순·오래된 순 정렬
- 시민 의견 페이지네이션
- 필터를 URL query string과 동기화해 새로고침·링크 공유 지원
- 리포트용 조사 개요와 방법론 표시
- `window.print()` 기반 PDF 저장 버튼
- 인쇄 전용 CSS 구성
  - 모바일 최대 너비 제한 해제
  - 버튼·필터 등 조작 요소 숨김
  - 표와 카드의 페이지 분할 최소화

## 구현 원칙

- 비율과 순위는 백엔드 값을 사용합니다.
- 막대그래프는 색상만으로 값을 구분하지 않고 숫자와 라벨을 함께 표시합니다.
- 후보자 페이지는 현재 기획 범위상 로그인 없이 구현하되, 실제 외부 공개 전 접근 정책을 다시 결정합니다.

## 완료 조건

- 통계 0건·1건·다건이 모두 깨지지 않고 표시됨
- 후보자 필터 조합과 의견 페이지 이동이 정상 작동함
- 필터가 URL에 남아 새로고침 후에도 복구됨
- 브라우저 인쇄 미리보기에서 리포트가 읽기 좋게 출력됨
- 표와 그래프를 보조기술에서도 이해할 수 있음

---

# 7. 캐릭터·애니메이션 교체 구조 준비

## 작업 목적

디자이너가 캐릭터·표정·배경을 전달하면 상태 관리 코드를 바꾸지 않고 자산만 연결할 수 있게 합니다.

## 구현할 내용

- 공통 `RiverCharacter` 인터페이스 정의
  - `riverId`
  - `grade`
  - `eventState`
  - `resultStatus`
- 3개 하천과 7개 수질등급의 자산 매핑 구조 정의
- 디자인 전에는 텍스트와 단순 SVG 도형으로 임시 캐릭터 표시
- 등급이 같으면 표정 유지, 등급이 바뀌면 표정 전환
- BOD 게이지 이동용 CSS transition 구조 구성
- 결과별 성공·퍼펙트·실패 애니메이션 영역 준비
- `prefers-reduced-motion` 사용자는 전환 효과를 줄이거나 제거
- 이미지에 대체 텍스트를 제공하거나 장식 이미지로 명확히 구분

## 디자이너에게 받아야 할 산출물

- 하천별 기본 캐릭터 3종
- 등급별 표정 7단계 또는 재조합 가능한 눈·입·눈썹 파츠
- 걱정·성공·퍼펙트 상태 정의
- 하천별 배경과 등급 변화 규칙
- SVG 원본과 필요한 경우 PNG/WebP 내보내기 파일
- 각 자산의 이름과 사용 상태가 적힌 전달표
- 애니메이션 시간과 움직임 설명

## 완료 조건

- 임시 캐릭터만으로 모든 상태를 테스트할 수 있음
- 최종 SVG를 매핑 파일에서 교체하면 모든 화면에 반영됨
- 없는 자산이 있어도 대체 화면이 표시됨
- 모션 감소 설정에서도 정보 손실 없이 이용 가능

---

# 8. 자동 테스트·접근성·반응형 검증

## 작업 목적

디자인 적용 전 기능 기준을 고정해 이후 CSS와 자산을 바꿔도 핵심 흐름이 깨지지 않게 합니다.

## 자동 테스트

- 단위 테스트
  - API 오류 변환
  - 챌린지 상태 전환
  - 오래된 시뮬레이션 응답 무시
  - 설문 검증
  - 표시 형식
- 컴포넌트 테스트
  - 정책 선택·취소
  - 돌발상황
  - 실패·성공·퍼펙트 결과
  - 개인정보 오류
  - 통계 빈 결과
  - 후보자 필터와 페이지네이션
- 전체 흐름 테스트
  - 랜딩 → 하천 선택 → 챌린지 → 제출 → 통계
  - 후보자 필터 → 의견 조회 → 인쇄

## 수동 검수 화면 크기

- 320px: 최소 지원 폭 확인
- 360px: 소형 Android 화면
- 390px: 일반 iPhone 화면
- 412×917px: 디자인 기준 화면
- 768px 이상: 가운데 정렬과 후보자 화면 확인

## 접근성 검수

- 키보드만으로 모든 기능 사용
- 포커스 표시와 모달 포커스 이동
- 폼 label과 오류 연결
- 텍스트와 배경 명도 대비
- 그래프에 숫자·텍스트 대안 제공
- 상태 변경을 필요한 경우 `aria-live`로 안내
- 터치 영역 최소 44×44px
- 모션 감소 설정 지원

## 완료 조건

- 타입 검사·린트·단위·컴포넌트 테스트 통과
- 핵심 시민 흐름 자동 테스트 통과
- 기준 화면에서 가로 스크롤과 잘린 버튼이 없음
- 키보드와 스크린 리더에 필요한 이름·상태가 제공됨

---

# 9. 최종 디자인 적용

## 작업 목적

기능과 데이터 흐름을 유지한 채 디자이너의 최종 화면을 빠르게 반영합니다.

## 디자이너에게 받아야 할 정보

- 412×917px 기준 전체 화면 디자인
- 색상·타이포그래피·간격·모서리·그림자 토큰
- 버튼·카드·입력·모달의 상태별 디자인
  - 기본
  - 눌림/선택
  - 비활성
  - 오류
  - 로딩
- 로딩·빈 결과·네트워크 오류 화면
- 정책 카드의 선택·취소·예산 초과 상태
- 실패·성공·퍼펙트 결과 화면
- 3개 하천 캐릭터와 등급별 표정
- 통계 차트와 후보자 인쇄 화면
- 개발에 사용할 SVG·이미지·아이콘 원본

## 적용 순서

1. 디자인 토큰 교체
2. 공통 컴포넌트 외형 적용
3. 랜딩·하천 선택 화면 적용
4. 챌린지·돌발상황·결과 화면 적용
5. 시민 통계 적용
6. 후보자 대시보드·인쇄 화면 적용
7. 캐릭터·배경·애니메이션 연결
8. 디자이너와 412×917px 화면 비교 검수

## 완료 조건

- 기능 코드의 큰 변경 없이 토큰·컴포넌트·자산 중심으로 디자인이 적용됨
- 디자인의 기본·선택·오류·로딩·빈 상태가 모두 구현됨
- 412×917px 기준 주요 화면이 디자인과 일치함
- 작은 화면과 큰 화면에서도 콘텐츠가 깨지지 않음

---

# 10. 배포 연동과 운영 점검

## 작업 목적

실제 프론트 주소와 백엔드·Supabase 운영 환경을 연결해 전체 흐름을 최종 확인합니다.

## 구현·설정할 내용

- 프론트엔드 배포 환경에 `VITE_API_BASE_URL` 설정
- 확정된 프론트 HTTPS 주소를 백엔드 `ALLOWED_ORIGINS`에 설정
- 개발·미리보기·운영 환경값 분리
- SPA 직접 접근 시 404가 나지 않도록 호스팅 rewrite 설정
- 빌드 결과와 정적 자산 캐시 정책 확인
- 소스맵 공개 여부 결정
- 기본 SEO 메타 정보와 서비스명 `강 새로이` 적용
- 후보자 페이지 검색 노출 여부와 접근 정책 결정
- 개인정보를 분석 도구나 오류 로그에 보내지 않도록 점검

## 실제 운영 검증 순서

1. 프론트에서 게임 설정 불러오기
2. 정책 선택 후 서버 계산 결과 확인
3. 돌발상황 선택과 최종 결과 확인
4. 익명 의견 한 건 제출
5. 시민 통계에 즉시 반영 확인
6. 후보자 리포트와 의견 목록에 반영 확인
7. PDF 인쇄 미리보기 확인
8. 테스트 데이터 처리 방침에 따라 삭제 또는 유지
9. 백엔드 배포 사전 점검 전체 통과 확인

## 완료 조건

- 운영 주소에서 새로고침과 직접 URL 접근이 정상 작동함
- CORS 오류 없이 백엔드와 통신함
- 저장한 응답이 시민 통계와 후보자 리포트에 반영됨
- 필수 데모·비대표성·개인정보 안내가 관련 화면에 표시됨
- 모바일 실제 기기에서 핵심 흐름과 PDF 저장을 확인함

---

# 디자인 전 바로 시작할 범위

다음 작업은 디자인 없이 바로 구현합니다.

1. 프로젝트 기반과 라우팅
2. API 타입·클라이언트·mock 데이터
3. 모바일 앱 컨테이너와 디자인 토큰
4. 5개 라우트의 기능 중심 화면 뼈대
5. 챌린지 상태 관리와 서버 연동
6. 통계·후보자 필터·페이지네이션·인쇄 기능
7. 자동 테스트와 접근성 기반

이 단계에서는 색상과 장식 요소를 임시값으로 두고, 화면 구조도 최종 디자인처럼 고정하지 않습니다.

# 디자인이 나온 뒤 진행할 범위

1. 최종 색상·폰트·간격·아이콘 적용
2. 공통 컴포넌트 외형 교체
3. 캐릭터·배경·표정·애니메이션 연결
4. 화면별 세부 레이아웃과 반응형 보정
5. 통계 차트와 인쇄 화면 시각 디자인 적용
6. 디자이너와 기준 화면 비교 검수

# 구현 전에 확정하면 좋은 중요 사항

다음 항목은 1~8번 진행을 막지는 않지만 배포 전에는 확정해야 합니다.

1. 프론트엔드 배포 서비스와 최종 HTTPS 주소
2. `/candidate`를 공개 페이지로 둘지, 별도 인증을 추가할지 여부
3. 디자이너가 캐릭터를 완성 SVG로 줄지, 표정 파츠 조합 방식으로 줄지 여부
4. 실제 공개 시 사용할 하천 데이터와 데모 데이터 표시 방식
5. 운영 환경에 테스트로 제출한 응답을 삭제할지 유지할지 여부

## 권장 결정

- 빠른 MVP는 로그인 없이 `/candidate`를 제공하되 검색 노출을 막고, 실제 운영 전 인증 도입 여부를 다시 결정합니다.
- 캐릭터는 하천별 몸체 3종과 표정 파츠를 분리해 21개 완성 이미지를 따로 관리하지 않는 방식을 권장합니다.
- 첫 구현은 CSS 막대그래프를 사용하고, 복잡한 차트 요구가 생길 때만 차트 라이브러리를 추가합니다.
- 서버 데이터와 게임 계산은 항상 백엔드를 단일 기준으로 사용합니다.

# 이번 프론트엔드 MVP에서 제외할 것

- 로그인·회원가입·관리자 권한
- 실제 지도 API
- 실제 수질센서 실시간 화면
- 프론트에서 수행하는 BOD 예측이나 통계 재계산
- 이메일 자동 발송
- 서버에서 생성하는 복잡한 PDF
- 정치적 지지율로 오해될 수 있는 표현
